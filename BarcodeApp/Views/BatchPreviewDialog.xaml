<Window x:Class="BarcodeApp.Views.BatchPreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:BarcodeApp.Converters"
        Title="Batch Preview" 
        Height="600" 
        Width="900"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        Foreground="{DynamicResource MaterialDesignBody}"
        FontSize="13"
        FontFamily="Segoe UI"
        Background="{DynamicResource MaterialDesignPaper}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Resources/MaterialDesignStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <!-- Converters -->
            <converters:ObjectToVisibilityConverter x:Key="ObjectToVisibilityConverter"/>
            <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Batch Preview"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="{DynamicResource MaterialDesignBody}"/>
            <Button Grid.Column="1"
                    Content="×"
                    FontSize="24"
                    FontWeight="Bold"
                    Width="36"
                    Height="36"
                    Padding="0"
                    Command="{Binding CloseCommand}"
                    Style="{StaticResource MaterialDesignIconButton}"
                    ToolTip="Close"
                    Foreground="{DynamicResource MaterialDesignBody}"/>
        </Grid>

        <!-- Preview Area -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Items List -->
            <ScrollViewer Grid.Column="0" 
                         VerticalScrollBarVisibility="Auto"
                         Margin="0,0,16,0">
                <ItemsControl ItemsSource="{Binding PreviewItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{DynamicResource MaterialDesignDivider}"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Padding="8"
                                    Margin="0,0,0,8"
                                    Cursor="Hand"
                                    MouseDown="PreviewItem_MouseDown">
                                <StackPanel>
                                    <TextBlock Text="{Binding Index, StringFormat='Item {0}'}"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource MaterialDesignBody}"
                                               Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding Item.Data}"
                                               FontSize="12"
                                               Foreground="{DynamicResource MaterialDesignSecondaryTextBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding Item.Symbology, StringFormat='Type: {0}'}"
                                               FontSize="11"
                                               Foreground="{DynamicResource MaterialDesignSecondaryTextBrush}"/>
                                    <Image Source="{Binding PreviewImage}"
                                           MaxWidth="200"
                                           MaxHeight="100"
                                           Stretch="Uniform"
                                           Margin="0,8,0,0"
                                           Visibility="{Binding PreviewImage, Converter={StaticResource ObjectToVisibilityConverter}}"/>
                                    <Border BorderBrush="#D32F2F"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Padding="8"
                                            Margin="0,8,0,0"
                                            Background="#FFEBEE"
                                            Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Text="{Binding ErrorMessage, StringFormat='⚠ {0}'}"
                                                   FontSize="11"
                                                   Foreground="#D32F2F"
                                                   TextWrapping="Wrap"/>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Large Preview -->
            <Border Grid.Column="1"
                    BorderBrush="{DynamicResource MaterialDesignDivider}"
                    BorderThickness="2"
                    CornerRadius="6"
                    Padding="32"
                    Background="{DynamicResource MaterialDesignBackground}">
                <Grid>
                    <Image Source="{Binding SelectedPreview}"
                           Stretch="Uniform"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           MaxWidth="500"
                           MaxHeight="400"
                           Visibility="{Binding SelectedPreview, Converter={StaticResource ObjectToVisibilityConverter}}"/>
                    <StackPanel HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="{Binding SelectedItem.HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="⚠ Preview Failed"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="#D32F2F"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding SelectedItem.ErrorMessage}"
                                   FontSize="14"
                                   Foreground="{DynamicResource MaterialDesignSecondaryTextBrush}"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   MaxWidth="400"/>
                    </StackPanel>
                    <TextBlock Text="Select an item to preview"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontSize="16"
                               FontStyle="Italic"
                               Foreground="{DynamicResource MaterialDesignSecondaryTextBrush}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedPreview}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectedItem.HasError}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Border>
        </Grid>

        <!-- Close Button -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Close"
                    Command="{Binding CloseCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>

