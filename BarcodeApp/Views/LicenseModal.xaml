<UserControl x:Class="BarcodeApp.Views.LicenseModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:BarcodeApp.Converters"
             mc:Ignorable="d"
             Foreground="{DynamicResource MaterialDesignBody}"
             FontSize="13"
             FontFamily="Segoe UI">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Resources/MaterialDesignStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <!-- Converters -->
            <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
            <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
            <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border Background="{DynamicResource MaterialDesignPaper}"
            BorderBrush="{DynamicResource MaterialDesignDivider}"
            BorderThickness="2"
            CornerRadius="8"
            Padding="32"
            MinWidth="500"
            MaxWidth="600">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" ShadowDepth="4" Opacity="0.3" Color="Black"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Litsentsi seaded"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="{DynamicResource MaterialDesignBody}"/>
                <Button Grid.Column="1"
                        Content="×"
                        FontSize="24"
                        FontWeight="Bold"
                        Width="36"
                        Height="36"
                        Padding="0"
                        Command="{Binding CloseCommand}"
                        Style="{StaticResource MaterialDesignIconButton}"
                        ToolTip="Sulge"
                        Foreground="{DynamicResource MaterialDesignBody}"/>
            </Grid>

            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="0,0,0,24">
                <TextBlock Text="Sisesta oma litsentsivõti rakenduse aktiveerimiseks."
                           Foreground="{DynamicResource MaterialDesignBody}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,24"/>

                <TextBlock Text="Litsentsivõti" 
                           FontSize="12"
                           Foreground="{DynamicResource MaterialDesignSecondaryTextBrush}"
                           Margin="0,0,0,4"/>
                <TextBox Text="{Binding LicenseKey, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,0,16"
                         IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
                         KeyDown="TextBox_KeyDown"/>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           TextWrapping="Wrap"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                           Margin="0,0,0,16"/>

                <TextBlock Text="{Binding LicenseStatusText}"
                           Foreground="{DynamicResource MaterialDesignBody}"
                           Margin="0,0,0,24"/>

                <TextBlock Text="Masina ID:"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource MaterialDesignBody}"
                           Margin="0,8,0,4"/>
                <TextBox Text="{Binding MachineIdDisplay, Mode=OneWay}"
                         IsReadOnly="True"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,0,16"/>
            </StackPanel>

            <!-- Footer Buttons -->
            <StackPanel Grid.Row="2" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button Content="Aktiveeri"
                        Command="{Binding ActivateCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="0,0,8,0"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"/>
                <ProgressBar IsIndeterminate="True"
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                             Width="100"
                             Height="4"
                             Margin="0,0,8,0"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>

